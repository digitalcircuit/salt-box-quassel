# System details
system:
  # Performance tuning
  tuning:
    # Virtual memory
    swap:
      # Use a swapfile?  Disable if already set up or not needed
      - enabled: true
      # Size of swapfile
      - size: 2048
