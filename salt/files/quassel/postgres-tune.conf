# pgtune wizard
# Generated from http://pgtune.leopard.in.ua/
# [!] Important: do NOT include the following:
# checkpoint_segments, min_wal_size, max_wal_size,
# checkpoint_completion_target, wal_buffers, default_statistics_target
# You might want to increase maintenance_work_mem if you have spare RAM.
# [record settings here]
# DB Version:        {{ salt['postgres.version']() }}
# DB Type:           Web applications
# Total Memory:      {{ grains['mem_total'] }} MB
# Num. connections:  {{ salt['pillar.get']('system:tuning:postgres:max_connections', '100') }}
# [paste content here]

max_connections = {{ salt['pillar.get']('system:tuning:postgres:max_connections', '100') }}
shared_buffers = {{ salt['pillar.get']('system:tuning:postgres:shared_buffers', '512MB') }}
effective_cache_size = {{ salt['pillar.get']('system:tuning:postgres:effective_cache_size', '1536MB') }}
work_mem = {{ salt['pillar.get']('system:tuning:postgres:work_mem', '2621kB') }}
maintenance_work_mem = {{ salt['pillar.get']('system:tuning:postgres:maintenance_work_mem', '128MB') }}

# End generated settings
