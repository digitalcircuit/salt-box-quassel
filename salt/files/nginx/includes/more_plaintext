# More Plaintext
# ------------------------
# Specify the following in the server configuration file within the main server block
	## [More Plaintext]
	#include includes/more_plaintext;
	#

# Set these files as text/plain
location ~ \.(sh|conf|cfg|patch)$ {
	default_type text/plain;
}
